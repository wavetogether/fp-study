import * as R from 'ramda';
import S from 'sanctuary';
import {getUrl, urlTypes} from './lib/api';

/**
 * Make Function handleFetch
 * Requirements : 
 *  1. Should follow all steps of api flow in README
 *  2. All functions cannot be wrapped with {} (code block). 
 *  3. You cannot use native javascript for, map, filter, reduce ..., must use functions from Ramdajs
 * 
 * 
 * And using handleFetch,
 * Make Function getEmployees and createEmployee
 */